#include <stdio.h>
int main(){
	int a=10;
	int b=2;
	int c=7;
	int d=3;

	printf("sum=%d, diff=%d, product=%d, quotient=%d\n",
		a+b, a-b, a*b, a/b);

	printf("sum=%d, diff=%d, product=%d, quotient=%d\n",
		c+d, c-d, c*d, c/d);

	return 0;
}
